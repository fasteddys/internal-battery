@using React.AspNet;
@using UpDiddy.ViewModels
@using UpDiddyLib.Dto
@model BrowseJobsLocationViewModel

<div class="container">
    <div class="row">
        <div class="col-12">

            @Html.React("BrowseJobsByLocation", new { cities = Model.List })

        </div>
    </div>
</div>
