@using React.AspNet;

@Html.Partial("Admin/_AdminNavigation")
<div class="container">
    <div class="row">
        <div class="col-12">
            @Html.React("RecruitersGrid", new { })
        </div>
    </div>
</div>
