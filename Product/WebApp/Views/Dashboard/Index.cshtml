@using React.AspNet;
@using UpDiddy.ViewModels
@model DashboardViewModel

<div class="container dashboard-container">
    @Html.React("Dashboard", new { notifications = Model.Notifications })
</div>